# `src/enter/`

This directory is declared in the layout of the source document but no file
contents were embedded for it.

If this is an *external / unchanged artefact* package, install or copy it here
before running the project. See `DEPENDENCIES.md` at the repository root.
